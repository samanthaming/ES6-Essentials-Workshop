/** EXERCISE 1
 *
 * Create a function takes an array of numbers
 *  and returns the number multiplied by 2
 *
 * You must solve this using the ES6 array methods:
 *  map, filter, reduce, forEach, find, findIndex, some, every
 *
 */

function multiplyByTwo(nums) {

}

multiplyByTwo([2, 4, 6]);
// Output: [4, 8, 12]


/** EXERCISE 2
 *
 * Create a function that takes an array
 *  and remove all false values
 *
 * You must solve this using the ES6 array methods:
 *  map, filter, reduce, forEach, find, findIndex, some, every
 *
 */

function allTrue() {

}

allTrue([true, false, true, false]);
// Output: [true, true];


/** EXERCISE 3
 *
 * Create a function that takes an array of numbers
 *  and return the sum of it all
 *
 * You must solve this using the ES6 array methods:
 *  map, filter, reduce, forEach, find, findIndex, some, every
 *
 */

function sumAll(nums) {

}

sumAll([10, 20, 5, 4]);
// Output: 39



/** EXERCISE 4
 *
 * Catch and return Pikachu
 *
 * You must solve this using the ES6 array methods:
 *  map, filter, reduce, forEach, find, findIndex, some, every
 *
 */

function catchPikacu(str) {

}

catchPikacu('Pika Pika Pikachu');
// Output: pikachu


/** EXERCISE 3
 *
 * Make sure everyone is 19 or over to have a drink
 *
 * You must solve this using the ES6 array methods:
 *  map, filter, reduce, forEach, find, findIndex, some, every
 *
 */

function cheers(age) {

}

cheers([19, 21, 32, 4]);
// Output: false
